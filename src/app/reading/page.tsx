import ReadingList from '@/components/ReadingList';

export default function ReadingPage() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-start overflow-hidden pt-24">
            <ReadingList />
        </main>
    );
}
